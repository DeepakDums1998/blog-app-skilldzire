{
  "type": "module",
  "name": "functions",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "lint": "echo \"(no lint configured)\"",
    "start": "echo \"Run emulators from firebase/: firebase emulators:start --only functions,firestore\"",
    "serve": "firebase emulators:start --only functions,firestore"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "private": true
}
